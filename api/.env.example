# Server Configuration
PORT=3001
NODE_ENV=production

# Database PostgreSQL
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/budgetflow

# JWT Secret (générer avec: openssl rand -base64 64)
JWT_SECRET=your_super_secret_jwt_key_here_min_64_characters_long

# CORS Origin (URL de votre frontend)
CORS_ORIGIN=https://your-domain.com
